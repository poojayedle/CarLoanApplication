import { EmiStatusPipe } from './emi-status.pipe';

describe('EmiStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new EmiStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
